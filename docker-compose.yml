services:
  # Container Unificado - VNC + Playwright
  unified-service:
    build:
      context: .
      dockerfile: Dockerfile.unified
    container_name: gbp_automation_unified
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - TZ=America/Sao_Paulo
      - VNC_PASSWORD=playwright123
      - RESOLUTION=800x600
      - HEADLESS=false
    volumes:
      - playwright_data:/app/data
      - /dev/shm:/dev/shm
    ports:
      - "81:3002"    # Playwright API
      - "6080:81"    # noVNC web interface
      - "5902:5900"  # VNC port
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined

volumes:
  playwright_data: